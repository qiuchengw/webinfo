import { UserViewDirective } from './user-view.directive';

describe('UserViewDirective', () => {
  it('should create an instance', () => {
    const directive = new UserViewDirective();
    expect(directive).toBeTruthy();
  });
});
